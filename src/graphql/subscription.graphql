# import './fragments.graphql'

subscription TokenSub($id: ID!) {
    token(id: $id) {
        id
        symbol
        address
    }
}

subscription CreditBalances($account: ID!) {
    account(id: $account) {
        creditBalances {
            amount
        }
    }
}
