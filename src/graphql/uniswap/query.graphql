query Pairs($ids: [ID!]!) @api(name: uniswap) {
  pairs(where: { id_in: $ids }) {
    id
    reserveUSD
    totalSupply
    token0 {
      id
      decimals
      symbol
      totalLiquidity
    }
    token1 {
      id
      decimals
      symbol
      totalLiquidity
    }
  }
}
